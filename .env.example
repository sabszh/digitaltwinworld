NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# Directory for one consented journey per JSON file. Defaults to .data/sessions.
SESSION_DATA_PATH=
# Name of the local macOS CUPS queue for the 58 mm value-card printer.
THERMAL_PRINTER_NAME=
OPENAI_API_KEY=
# Optional legacy fallback for utility calls.
OPENAI_MODEL=
# Creative authoring needs the stronger default; scoring and reports stay cheap.
DILEMMA_MODEL=gpt-5.6-terra
UTILITY_MODEL=gpt-5.6-luna
